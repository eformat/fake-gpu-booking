= Module 3: RHOAI Hardware Profiles
:navtitle: Module 3: Hardware Profiles
:toc: macro
:toclevels: 1
:icons: font

Hardware Profiles in Red Hat OpenShift AI define the compute resources available for workbenches and model serving. In this workshop environment, they connect GPU reservations from the booking app to the RHOAI workbench experience -- when you reserve a GPU, a corresponding HardwareProfile appears so you can launch a workbench with that GPU type.

This is where **GPU as a Service** meets **Data Science**: you reserve GPU capacity through the booking app, and the reservation automatically surfaces as a Hardware Profile that data scientists select when launching workbenches. The data scientist doesn't need to know about Kueue, ClusterQueues, or Cohorts -- they just pick a profile and get a GPU-enabled notebook.

In this module, you'll create a reservation, explore the Hardware Profiles it generates, launch a workbench with a reserved GPU, and verify GPU access from inside the notebook environment.

== Learning objectives

By the end of this module, you'll be able to:

* Create a GPU reservation and inspect the Hardware Profiles it generates
* Understand how profiles map to Kueue ClusterQueues and ResourceFlavors
* Launch an RHOAI workbench using a reserved GPU Hardware Profile
* Verify GPU access from inside a Data Science workbench using `nvidia-smi`
* Explain the relationship between bookings, profiles, and scheduling

== How Hardware Profiles connect to Kueue

The reservation sync creates Hardware Profiles with specific resource limits that match the Kueue queue configuration:

[cols="1,1,1,1",options="header"]
|===
| Hardware Profile | GPU Resource | CPU | Memory

| `unreserved-gpu`
| 1x `nvidia.com/gpu`
| 2
| 8Gi

| `unreserved-mig-71gb`
| 1x `nvidia.com/mig-3g.71gb`
| 2
| 8Gi

| `unreserved-mig-35gb`
| 1x `nvidia.com/mig-2g.35gb`
| 1
| 4Gi

| `unreserved-mig-18gb`
| 1x `nvidia.com/mig-1g.18gb`
| 1
| 4Gi

| `default-profile`
| none (CPU only)
| 2
| 8Gi
|===

When you make a reservation through the booking app, additional per-user profiles are created that point to your reserved ClusterQueue.

== Exercise 1: Reserve a GPU and check the Hardware Profiles it creates

We cleaned up all resources at the end of Module 2, so we need a fresh GPU reservation before we can launch a workbench with a reserved profile.

[IMPORTANT]
====
**Acting as: `{user-1}`** +
Log in to the OpenShift Console at `{openshift_console_url}` with username `{user-1}` and password `{password}`.
====

=== Create a GPU reservation

. Navigate to **GPU Booking** > **Bookings** in the console menu.

. Select **today's date** in the calendar.

. In the **GB200 NVL Full GPU** row, reserve **1** GPU by clicking on an available slot.

. Wait for the reservation to appear as a red slot in the calendar.
+
The Reservation Sync runs every 10 minutes. Once it picks up your booking, it creates a per-user ClusterQueue, LocalQueue, and Hardware Profiles in your namespace.

=== Check the Hardware Profiles

. Log in to the OpenShift CLI:
+
[.copypaste]
[source,bash,subs="attributes"]
----
oc login {openshift_api_url} -u {user-1} -p {password}
----

. List Hardware Profiles in your namespace:
+
[.copypaste]
[source,bash,subs="attributes"]
----
oc -n user-{user-1} get hardwareprofiles
----
+
You should see a `reserved-gpu` profile alongside the default unreserved profiles. This profile was created by the booking app's Reservation Sync when it processed your booking.

. Inspect the reserved Hardware Profile:
+
[.copypaste]
[source,bash,subs="attributes"]
----
oc -n user-{user-1} get hardwareprofile reserved-gpu -o yaml | grep -A 15 "spec:"
----
+
The reserved profile specifies CPU, memory, and GPU resource limits that match your user-specific ClusterQueue. When a workbench uses this profile, its workload is admitted to your reserved queue with protected `nominalQuota` -- not the shared unreserved pool.

=== Verify

&#10003; GPU reservation created in the booking calendar +
&#10003; `reserved-gpu` HardwareProfile exists in your namespace +
&#10003; Profile spec shows GPU resource limits pointing to the reserved LocalQueue

== Exercise 2: Explore default Hardware Profiles

. List all Hardware Profiles across namespaces:
+
[.copypaste]
[source,bash]
----
oc get hardwareprofiles -A
----

. Inspect the unreserved GPU profile:
+
[.copypaste]
[source,bash,subs="attributes"]
----
oc -n user-{user-user1} get hardwareprofile unreserved-gpu -o yaml
----
+
Note the `spec.resources` section showing CPU, memory, and GPU limits.

. Compare with the default (CPU-only) profile:
+
[.copypaste]
[source,bash,subs="attributes"]
----
oc -n user-{user-user1} get hardwareprofile default-profile -o yaml
----

=== Verify

&#10003; Multiple Hardware Profiles exist per namespace +
&#10003; GPU profiles include `nvidia.com/gpu` or MIG resource limits +
&#10003; Default profile has no GPU resource

== Exercise 3: Launch a workbench with the reserved GPU profile

This is where GPU as a Service connects to the Data Science workflow. Your GPU reservation created a `reserved-gpu` Hardware Profile -- now you'll use it to launch a workbench with guaranteed GPU access.

. Open the Red Hat OpenShift AI dashboard:
+
`{openshift_ai_url}`
+
Log in as `{user-1}` with password `{password}`.

. Navigate to **Data Science Projects** and select your project.

. Click **Create workbench** and configure:
* **Name:** `gpu-workbench`
* **Image Selection:** Training | Jupyter | PyTorch | CUDA | Python
* **Hardware Profile:** Select **Reserved GPU** (Project-scoped)
+
The **Reserved GPU** profile appears because you created a reservation in Exercise 1. It targets your user-specific ClusterQueue with protected `nominalQuota`, meaning your workbench GPU cannot be preempted by unreserved workloads.

. Click **Create workbench** and wait for it to reach **Ready** status.

=== Verify GPU access from the workbench

. From the CLI, verify the workbench pod has the GPU allocated:
+
[.copypaste]
[source,bash,subs="attributes"]
----
oc -n user-{user-1} exec $(oc -n user-{user-1} get pod -l app=gpu-workbench -o name) -- nvidia-smi
----
+
You should see the simulated NVIDIA GB200 NVL GPU with its memory and utilization. This confirms the fake-gpu-operator injected `nvidia-smi` into the workbench pod and the GPU resource is allocated.

. Verify that the workload was admitted to the **reserved** queue, not the unreserved pool:
+
[.copypaste]
[source,bash,subs="attributes"]
----
oc -n user-{user-1} get workloads -o custom-columns=\
NAME:.metadata.name,\
QUEUE:.spec.queueName,\
ADMITTED:.status.conditions[0].status
----
+
The `QUEUE` column should show `reserved` -- confirming the workbench is running under your protected reservation, not competing for shared resources.

=== Verify GPU from inside the notebook

You can also verify GPU access directly from within the JupyterLab environment:

. In the OpenShift AI dashboard, click **Open** on the `gpu-workbench` workbench to launch JupyterLab.

. Open a **Terminal** in JupyterLab (File > New > Terminal) and run:
+
[source,bash]
----
nvidia-smi
----
+
You should see the same GPU information as the CLI check above.

. In a new **Notebook**, run a quick PyTorch GPU check:
+
[source,python]
----
import torch
print(f"CUDA available: {torch.cuda.is_available()}")
print(f"GPU count: {torch.cuda.device_count()}")
print(f"GPU name: {torch.cuda.get_device_name(0)}")
----
+
This confirms the Data Science stack (PyTorch + CUDA) can see the reserved GPU -- the same experience a data scientist would have on a real GPU cluster.

=== Verify

&#10003; Workbench started with the **Reserved GPU** Hardware Profile +
&#10003; `nvidia-smi` shows GPU info from both CLI and inside the notebook +
&#10003; Workload admitted to the `reserved` queue +
&#10003; PyTorch detects the GPU inside the workbench

== Exercise 4: Observe profile lifecycle

Hardware Profiles created by the booking app follow the reservation lifecycle:

. When a reservation is active, the corresponding HardwareProfile exists in your namespace.

. When a reservation expires, the expiration cleaner removes the HardwareProfile as part of the two-phase drain.

. Watch for profile changes:
+
[.copypaste]
[source,bash,subs="attributes"]
----
oc -n user-{user-user1} get hardwareprofiles -w
----
+
Leave this running in a terminal and observe as reservations change. Press `Ctrl+C` to stop watching.

. Clean up the workbench when done:
+
In the OpenShift AI dashboard, stop and delete the `gpu-workbench` workbench from your project.

=== Verify

&#10003; Understand that HardwareProfiles follow reservation lifecycle +
&#10003; Workbench cleaned up

== Module summary

**What you accomplished:**

* Created a GPU reservation and inspected the Hardware Profiles it generates
* Explored the default and reservation-created Hardware Profiles
* Launched an RHOAI workbench using the reserved GPU Hardware Profile
* Verified GPU access from the CLI, the JupyterLab terminal, and from PyTorch
* Understood the profile lifecycle tied to booking reservations

**Key takeaways:**

* Hardware Profiles bridge **GPU as a Service** (booking + Kueue) and **Data Science** (workbenches + notebooks)
* The reserved profile directs workloads to your protected ClusterQueue -- the data scientist doesn't need to know about Kueue
* `nvidia-smi` and PyTorch work inside the workbench exactly as they would on a real GPU cluster
* The booking app creates and removes profiles automatically as reservations change

**Next:** Module 4 dives into Kueue concepts -- ClusterQueues, Cohorts, and the resource sharing model.
